<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.webuy">

    <application
        android:allowBackup="true"
        android:icon="@drawable/w"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login"></activity>
        <activity android:name=".UneOffre" />
        <activity android:name=".ListeOffresMagasin" />
        <activity android:name=".Messages" />
        <activity
            android:name=".ListeOffres"
            android:label="@string/title_activity_liste_offres"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListeMagasins" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>